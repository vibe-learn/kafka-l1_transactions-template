        # kafka — Транзакции и end-to-end exactly-once

        Homework template for the Vibe Learn lesson **l1_transactions** (Транзакции и end-to-end exactly-once).

        ## Task

        Реализуй transactional producer на Go (confluent-kafka-go). Сервис читает топик
`usage_events`, агрегирует по `customer_id` за batch и пишет в `hourly_charges`
с EOS-гарантиями через `sendOffsetsToTransaction()`.

Chaos-тест (входит в шаблон):
- Запускает processor, посылает 50 usage_events в 5 batches по 10.
- После каждого второго batch убивает процессор mid-transaction (SIGKILL).
- Перезапускает processor.
- Читает `hourly_charges` с `isolation.level=read_committed` и проверяет:
  - Каждый customer_id встречается ровно столько раз, сколько batches он вошёл.
  - Нет дублей (duplicate charge_id).
  - Нет потерь (sum of charges == sum of input usage).

CI-assert: `go test ./... -run TestTransactionalEOS` должен проходить за < 30 сек.

        ## How to work

        1. The Vibe Learn platform creates a copy of this repo in your GitHub account when you click «Начать домашку» on the lesson.
        2. Clone it locally, complete the TODOs in `main.go`, and push.
        3. CI (`.github/workflows/ci.yml`) runs the tests on every push. The Vibe Learn platform listens to the result via webhook and unlocks the lesson when CI passes.

        ## Local prerequisites

        - Go 1.22+
        - Docker + docker-compose (for the local Kafka cluster, when the lesson requires it)

        ## Running

        ```bash
        go test ./...
        ```

        ## Notes for the author / maintainer

        This is a baseline template generated by the platform. The substantive task content for this lesson lives in the platform's `lesson.yaml` and will be expanded in future iterations.
